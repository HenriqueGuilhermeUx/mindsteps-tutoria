{
  "name": "mindsteps",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "cd client && npm run dev",
    "build": "cd client && npm install && npm run build"
  },
  "engines": {
    "node": ">=20.0.0 <25.0.0"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "bcrypt": "^5.1.1",
    "jsonwebtoken": "^9.0.2",
    "zod": "^3.23.0",
    "@supabase/supabase-js": "^2.43.0",
    "openai": "^4.47.0"
  }
}
