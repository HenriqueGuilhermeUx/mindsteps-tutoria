import { supabase } from '../db/index.js'
export type TransferCheckInput={userId:string;subject?:string|null;skill?:string|null;source?:string;assistedScore?:number|null;independentScore?:number|null;helpLevel?:number;attemptBeforeHelp?:boolean;completed?:boolean;metadata?:Record<string,unknown>}
export async function saveTransferCheck(input:TransferCheckInput){const{data,error}=await supabase.from('mindsteps_transfer_checks').insert({user_id:input.userId,subject:input.subject||null,skill:input.skill||null,source:input.source||'tutor',assisted_score:input.assistedScore??null,independent_score:input.independentScore??null,help_level:input.helpLevel??0,attempt_before_help:input.attemptBeforeHelp??false,completed:input.completed??true,metadata:input.metadata||{}}).select('*').single();if(error)throw new Error(error.message);return data}
export async function listTransferChecks(userId:string,limit=50){const{data,error}=await supabase.from('mindsteps_transfer_checks').select('*').eq('user_id',userId).order('created_at',{ascending:false}).limit(limit);if(error)throw new Error(error.message);return data||[]}
const avg=(xs:number[])=>xs.length?xs.reduce((a,b)=>a+b,0)/xs.length:0
const key=(r:any)=>`${String(r.subject||'geral').toLowerCase()}::${String(r.skill||'*').toLowerCase()}`
export function summarizeIndependence(rows:any[]){const completed=rows.filter(r=>r.completed);const assistedRows=completed.filter(r=>r.assisted_score!=null);const independentRows=completed.filter(r=>r.independent_score!=null);const direct=completed.filter(r=>r.assisted_score!=null&&r.independent_score!=null).map(r=>({assisted:Number(r.assisted_score),independent:Number(r.independent_score)}));const paired=[...direct];for(const independent of independentRows.filter(r=>r.assisted_score==null)){const before=(a:any)=>new Date(a.created_at).getTime()<=new Date(independent.created_at).getTime();const exact=assistedRows.find(a=>key(a)===key(independent)&&before(a));const subjectFallback=assistedRows.find(a=>String(a.subject||'').toLowerCase()===String(independent.subject||'').toLowerCase()&&before(a));const match=exact||subjectFallback;if(match)paired.push({assisted:Number(match.assisted_score),independent:Number(independent.independent_score)})}const assisted=paired.length?avg(paired.map(p=>p.assisted)):avg(assistedRows.map(r=>Number(r.assisted_score)));const independent=independentRows.length?avg(independentRows.map(r=>Number(r.independent_score))):0;const pairedIndependent=paired.length?avg(paired.map(p=>p.independent)):independent;const gap=paired.length?assisted-pairedIndependent:0;const attemptShare=completed.length?completed.filter(r=>r.attempt_before_help).length/completed.length:0;const avgHelp=completed.length?avg(completed.map(r=>Number(r.help_level||0))):0;const independenceScore=Math.max(0,Math.min(100,Math.round((independent||0)*.7+attemptShare*20+Math.max(0,10-avgHelp*2))));const offloadingRisk=paired.length<3?'unknown':gap>=20||attemptShare<.35?'high':gap>=10||attemptShare<.6?'medium':'low';return{checks:completed.length,assistedChecks:assistedRows.length,independentChecks:independentRows.length,comparableChecks:paired.length,assistedScore:Math.round(assisted),independentScore:Math.round(independent),assistanceGap:Math.round(gap),attemptBeforeHelpShare:Math.round(attemptShare*100),averageHelpLevel:Number(avgHelp.toFixed(1)),independenceScore,offloadingRisk,notice:paired.length<1?'Faça um treino com o Tutor e depois um check sem ajuda para medir o gap de assistência.':paired.length<3?'O primeiro gap assistido × independente já existe; ainda precisamos de mais evidências para estimar dependência com confiança.':'O objetivo é reduzir o gap de assistência sem retirar ajuda necessária.'}}
