import { useEffect,useMemo,useRef,useState } from 'react'
import { useNavigate } from 'react-router-dom'
import { ArrowRight,CheckCircle2,Clock3,Flag,Pause,Play,RotateCcw,TimerReset } from 'lucide-react'
import { useEnemStore,type EnemArea,type EnemSimulationAreaResult } from '@/stores/enem'
import { independentLearningApi } from '@/lib/independentLearningApi'
type Area=Exclude<EnemArea,'Redação'>;type SimQuestion={id:string;area:Area;skill:string;text:string;options:string[];answer:number;difficulty:1|2|3};type AnswerMap=Record<string,number|undefined>
const questions:SimQuestion[]=[{id:'sim-l1',area:'Linguagens',skill:'Inferência',text:'Quando o sentido principal depende de pistas não literais, a habilidade central é:',options:['memorização','inferência','cópia','classificação'],answer:1,difficulty:2},{id:'sim-l2',area:'Linguagens',skill:'Leitura crítica',text:'Diante de dois textos com teses opostas, o melhor critério de comparação é:',options:['tamanho','quantidade de palavras difíceis','qualidade dos argumentos e evidências','ordem de publicação'],answer:2,difficulty:3},{id:'sim-h1',area:'Humanas',skill:'Fonte histórica',text:'Uma fonte produzida por um governo deve ser analisada considerando:',options:['apenas a data','contexto, autoria e finalidade','somente seu valor artístico','se concorda com o leitor'],answer:1,difficulty:2},{id:'sim-h2',area:'Humanas',skill:'Cidadania',text:'Uma política pública se diferencia de uma decisão privada porque:',options:['não possui objetivo','responde a problemas coletivos com ação institucional','vale apenas para empresas','não envolve recursos públicos'],answer:1,difficulty:2},{id:'sim-n1',area:'Natureza',skill:'Ecologia',text:'A remoção de um predador de topo pode causar:',options:['equilíbrio automático','efeitos em cascata na cadeia alimentar','somente aumento de decompositores','nenhuma mudança'],answer:1,difficulty:2},{id:'sim-n2',area:'Natureza',skill:'Evidência científica',text:'Uma hipótese ganha força científica quando:',options:['é repetida muitas vezes','é apoiada por evidências reproduzíveis','é defendida por uma pessoa famosa','não pode ser testada'],answer:1,difficulty:3},{id:'sim-m1',area:'Matemática',skill:'Proporção',text:'Se 4 ingressos custam R$ 60 no mesmo preço unitário, 10 custam:',options:['R$ 120','R$ 130','R$ 140','R$ 150'],answer:3,difficulty:2},{id:'sim-m2',area:'Matemática',skill:'Modelagem',text:'Um serviço cobra R$ 12 fixos mais R$ 3 por unidade. Para 6 unidades, o total é:',options:['R$ 18','R$ 24','R$ 30','R$ 36'],answer:2,difficulty:2}]
const areaOrder:Area[]=['Linguagens','Humanas','Natureza','Matemática']
export function EnemSimulationPage(){const navigate=useNavigate();const{simulations,recordSimulation,recordAttempt}=useEnemStore();const[phase,setPhase]=useState<'intro'|'run'|'review'|'result'>('intro');const[index,setIndex]=useState(0);const[paused,setPaused]=useState(false);const[elapsed,setElapsed]=useState(0);const[answers,setAnswers]=useState<AnswerMap>({});const[marked,setMarked]=useState<string[]>([]);const[reviewChanges,setReviewChanges]=useState(0);const[result,setResult]=useState<{correct:number;strategy:number;areas:EnemSimulationAreaResult[]}|null>(null);const questionStartedAt=useRef(Date.now());const timeByQuestion=useRef<Record<string,number>>({});useEffect(()=>{if(phase!=='run'||paused)return;const id=window.setInterval(()=>setElapsed(e=>e+1),1000);return()=>window.clearInterval(id)},[phase,paused]);const q=questions[index];const answeredCount=Object.values(answers).filter(v=>v!==undefined).length;const saveTime=()=>{if(!q)return;const seconds=Math.max(1,Math.round((Date.now()-questionStartedAt.current)/1000));timeByQuestion.current[q.id]=(timeByQuestion.current[q.id]||0)+seconds;questionStartedAt.current=Date.now()};const choose=(o:number)=>phase==='run'&&setAnswers(a=>({...a,[q.id]:o}));const toggleMark=()=>setMarked(m=>m.includes(q.id)?m.filter(id=>id!==q.id):[...m,q.id]);const go=(next:number)=>{saveTime();setIndex(Math.max(0,Math.min(questions.length-1,next)))};const skip=()=>{saveTime();if(index===questions.length-1)setPhase('review');else setIndex(i=>i+1)};const reviewSelect=(question:SimQuestion,option:number)=>{const before=answers[question.id];if(before!==undefined&&before!==option)setReviewChanges(c=>c+1);setAnswers(a=>({...a,[question.id]:option}))};const finish=()=>{const correct=questions.filter(item=>answers[item.id]===item.answer).length;const skipped=questions.length-answeredCount;const areas:EnemSimulationAreaResult[]=areaOrder.map(area=>{const qs=questions.filter(item=>item.area===area);return{area,answered:qs.filter(item=>answers[item.id]!==undefined).length,correct:qs.filter(item=>answers[item.id]===item.answer).length,skipped:qs.filter(item=>answers[item.id]===undefined).length,marked:qs.filter(item=>marked.includes(item.id)).length,secondsSpent:qs.reduce((s,item)=>s+(timeByQuestion.current[item.id]||0),0)}});questions.forEach(item=>{const value=answers[item.id];if(value!==undefined)recordAttempt({questionId:item.id,area:item.area,skill:item.skill,correct:value===item.answer,difficulty:item.difficulty})});const accuracy=correct/questions.length;const completion=answeredCount/questions.length;const markControl=marked.length?Math.max(0,1-Math.max(0,marked.length-3)/questions.length):1;const strategy=Math.round((accuracy*.55+completion*.25+markControl*.2)*100);recordSimulation({elapsedSeconds:elapsed,markedQuestions:marked.length,answeredQuestions:answeredCount,correctQuestions:correct,skippedQuestions:skipped,reviewChanges,areaResults:areas,strategyScore:strategy});void independentLearningApi.save({subject:'ENEM',skill:'Simulado multidisciplinar',source:'enem_simulation_no_help',independentScore:Math.round(accuracy*100),helpLevel:0,attemptBeforeHelp:true,completed:true,metadata:{questions:questions.length,answered:answeredCount,strategy,elapsedSeconds:elapsed}}).catch(()=>undefined);setResult({correct,strategy,areas});setPhase('result')};const reset=()=>{setPhase('intro');setIndex(0);setPaused(false);setElapsed(0);setAnswers({});setMarked([]);setReviewChanges(0);setResult(null);timeByQuestion.current={};questionStartedAt.current=Date.now()};const recent=simulations[0];const currentArea=useMemo(()=>q?.area,[q]);return <main className="ms-screen space-y-4 py-4"><section className="rounded-[30px] bg-gradient-to-br from-amber-500 via-orange-600 to-rose-600 p-5 text-white"><div className="flex items-center justify-between"><div><p className="text-xs font-black uppercase tracking-[.18em] text-amber-100">Modo Prova · sem IA</p><h1 className="mt-2 text-2xl font-black">Treine o que você consegue sozinho.</h1></div><TimerReset className="h-8 w-8"/></div><p className="mt-3 text-sm leading-6 text-white/80">Sem pistas e sem Tutor durante a prova. O resultado vira evidência real de independência.</p></section>{phase==='intro'&&<><section className="rounded-3xl bg-white p-5 shadow-sm"><div className="grid grid-cols-3 gap-2 text-center"><Mini value={questions.length} label="questões"/><Mini value={4} label="áreas"/><Mini value={simulations.length} label="sessões"/></div>{recent?.strategyScore!==undefined&&<div className="mt-4 rounded-2xl bg-indigo-50 p-4"><p className="text-xs font-black uppercase text-indigo-700">Última estratégia</p><p className="mt-1 text-2xl font-black text-indigo-950">{recent.strategyScore}/100</p></div>}<button onClick={()=>{setPhase('run');questionStartedAt.current=Date.now()}} className="mt-4 flex w-full items-center justify-center gap-2 rounded-2xl bg-slate-950 py-4 text-sm font-black text-white"><Play className="h-4 w-4"/>Iniciar sem ajuda</button></section><section className="rounded-3xl bg-emerald-50 p-5"><p className="text-xs font-black uppercase tracking-wider text-emerald-700">Conta para sua autonomia</p><p className="mt-2 text-sm leading-6 text-emerald-900/75">Ao finalizar, o desempenho entra em Minha Autonomia como check independente. Treino assistido e prova sem ajuda não são a mesma coisa.</p></section></>}{phase==='run'&&<section className="rounded-3xl bg-white p-5 shadow-sm"><div className="flex items-center justify-between"><div><p className="text-xs font-black uppercase text-slate-400">{currentArea} · {q.skill}</p><h2 className="mt-1 text-lg font-black">Questão {index+1}/{questions.length}</h2></div><div className="flex items-center gap-2 text-amber-700"><Clock3 className="h-4 w-4"/><span className="text-sm font-black">{String(Math.floor(elapsed/60)).padStart(2,'0')}:{String(elapsed%60).padStart(2,'0')}</span></div></div><p className="mt-4 text-base font-black leading-7">{q.text}</p><div className="mt-4 space-y-2">{q.options.map((option,i)=><button key={option} onClick={()=>choose(i)} className={`w-full rounded-2xl border p-4 text-left text-sm font-bold ${answers[q.id]===i?'border-indigo-400 bg-indigo-50':'border-slate-100 bg-slate-50'}`}>{String.fromCharCode(65+i)}. {option}</button>)}</div><div className="mt-4 grid grid-cols-3 gap-2"><button onClick={toggleMark} className={`flex items-center justify-center gap-1 rounded-2xl py-3 text-xs font-black ${marked.includes(q.id)?'bg-rose-100 text-rose-800':'bg-slate-100'}`}><Flag className="h-3 w-3"/>Marcar</button><button onClick={skip} className="rounded-2xl bg-amber-50 py-3 text-xs font-black text-amber-800">Pular</button><button onClick={()=>setPaused(p=>!p)} className="flex items-center justify-center gap-1 rounded-2xl bg-slate-950 py-3 text-xs font-black text-white">{paused?<Play className="h-3 w-3"/>:<Pause className="h-3 w-3"/>}{paused?'Retomar':'Pausar'}</button></div><div className="mt-3 flex justify-between"><button disabled={index===0} onClick={()=>go(index-1)} className="text-xs font-black text-slate-400">← anterior</button>{index===questions.length-1?<button onClick={()=>{saveTime();setPhase('review')}} className="text-xs font-black text-indigo-700">Revisar prova →</button>:<button onClick={()=>go(index+1)} className="text-xs font-black text-indigo-700">próxima →</button>}</div></section>}{phase==='review'&&<><section className="rounded-3xl bg-white p-5 shadow-sm"><p className="text-xs font-black uppercase text-violet-700">Revisão estratégica</p><h2 className="mt-2 text-xl font-black">Decida sem receber resposta.</h2><div className="mt-4 space-y-3">{[...questions].sort((a,b)=>Number(marked.includes(b.id))-Number(marked.includes(a.id))).map(item=><div key={item.id} className="rounded-2xl border p-4"><p className="text-xs font-black text-slate-500">{item.area} · {item.skill}</p><p className="mt-2 text-sm font-bold">{item.text}</p><div className="mt-3 grid grid-cols-2 gap-2">{item.options.map((option,i)=><button key={option} onClick={()=>reviewSelect(item,i)} className={`rounded-xl p-2 text-left text-xs font-bold ${answers[item.id]===i?'bg-indigo-100':'bg-slate-50'}`}>{String.fromCharCode(65+i)}. {option}</button>)}</div></div>)}</div></section><button onClick={finish} className="w-full rounded-2xl bg-slate-950 py-4 text-sm font-black text-white">Finalizar e registrar autonomia</button></>}{phase==='result'&&result&&<><section className="rounded-3xl bg-emerald-50 p-5"><div className="flex items-center gap-2 text-emerald-700"><CheckCircle2 className="h-5 w-5"/><span className="text-xs font-black uppercase">Evidência independente registrada</span></div><h2 className="mt-2 text-2xl font-black text-emerald-950">{result.correct}/{questions.length} sem ajuda · estratégia {result.strategy}/100</h2><p className="mt-2 text-sm leading-6 text-emerald-900/70">Esse resultado agora conta para sua leitura de autonomia. Ele não é misturado silenciosamente com desempenho assistido.</p></section><section className="space-y-2">{result.areas.map(area=><div key={area.area} className="rounded-2xl bg-white p-4 shadow-sm"><div className="flex justify-between"><p className="text-sm font-black">{area.area}</p><p className="text-sm font-black text-slate-500">{area.correct}/{Math.max(1,area.answered)}</p></div></div>)}</section><div className="grid grid-cols-2 gap-3"><button onClick={reset} className="flex items-center justify-center gap-2 rounded-2xl border bg-white py-3 text-sm font-black"><RotateCcw className="h-4 w-4"/>Novo</button><button onClick={()=>navigate('/independencia')} className="flex items-center justify-center gap-2 rounded-2xl bg-indigo-700 py-3 text-sm font-black text-white">Minha autonomia <ArrowRight className="h-4 w-4"/></button></div></>}</main>}
function Mini({value,label}:{value:number;label:string}){return <div className="rounded-2xl bg-slate-50 p-3"><p className="text-xl font-black">{value}</p><p className="text-[10px] font-bold text-slate-400">{label}</p></div>}
