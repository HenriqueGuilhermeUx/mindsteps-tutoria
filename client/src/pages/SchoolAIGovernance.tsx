import { useEffect,useMemo,useState } from 'react'
import { AlertTriangle,Building2,CheckCircle2,Save,ShieldCheck } from 'lucide-react'
import { platformResponsibleApi,type ManagedInstitution } from '@/lib/responsiblePlatformApi'

export function SchoolAIGovernancePage(){const[institutions,setInstitutions]=useState<ManagedInstitution[]>([]);const[selected,setSelected]=useState('');const[policy,setPolicy]=useState<Record<string,unknown>>({});const[readiness,setReadiness]=useState<Record<string,unknown>>({});const[incidents,setIncidents]=useState<Array<Record<string,unknown>>>([]);const[error,setError]=useState('');const[saving,setSaving]=useState(false);useEffect(()=>{platformResponsibleApi.institutions().then(r=>{setInstitutions(r.institutions);if(r.institutions[0])setSelected(r.institutions[0].id)}).catch(e=>setError(e.message))},[]);useEffect(()=>{if(!selected)return;Promise.all([platformResponsibleApi.schoolPolicy(selected),platformResponsibleApi.readiness(selected),platformResponsibleApi.incidents(selected)]).then(([p,r,i])=>{setPolicy(p.policy||{});setReadiness(r);setIncidents(i.incidents)}).catch(e=>setError(e.message))},[selected]);const score=Number(readiness.score||0);const allowed=useMemo(()=>Array.isArray(policy.allowed_systems)?policy.allowed_systems as string[]:[],[policy]);const prohibited=useMemo(()=>Array.isArray(policy.prohibited_uses)?policy.prohibited_uses as string[]:[],[policy]);const save=async()=>{if(!selected)return;setSaving(true);try{const body={title:String(policy.title||'Política de IA da instituição'),status:String(policy.status||'draft'),allowedSystems:allowed,prohibitedUses:prohibited,ageRules:policy.age_rules||{},dataRules:policy.data_rules||{},humanReviewRules:policy.human_review_rules||{},incidentContact:policy.incident_contact?String(policy.incident_contact):null};const r=await platformResponsibleApi.saveSchoolPolicy(selected,body);setPolicy(r.policy);setReadiness(await platformResponsibleApi.readiness(selected))}catch(e){setError(e instanceof Error?e.message:'Erro ao salvar')}finally{setSaving(false)}};return <main className="mx-auto w-full max-w-7xl space-y-6 px-4 py-8"><section className="rounded-[32px] bg-slate-950 p-7 text-white"><div className="flex items-start justify-between gap-4"><div><p className="text-xs font-black uppercase tracking-[.18em] text-cyan-300">School AI Governance</p><h1 className="mt-2 text-3xl font-black">Governar IA é parte da gestão pedagógica.</h1><p className="mt-3 max-w-3xl text-sm leading-6 text-slate-300">Defina sistemas autorizados, limites, revisão humana, proteção de dados e resposta a incidentes. O centro não substitui a política institucional; ele a torna operável.</p></div><ShieldCheck className="h-9 w-9 text-cyan-300"/></div></section>{error&&<div className="rounded-2xl bg-rose-50 p-4 text-sm font-bold text-rose-700">{error}</div>}<section className="flex flex-wrap items-center gap-3 rounded-3xl bg-white p-5 shadow-sm"><Building2 className="h-5 w-5 text-slate-500"/><select value={selected} onChange={e=>setSelected(e.target.value)} className="min-w-64 rounded-xl bg-slate-50 px-3 py-2 text-sm font-bold outline-none">{institutions.map(i=><option key={i.id} value={i.id}>{i.name}</option>)}</select><div className="ml-auto rounded-2xl bg-cyan-50 px-4 py-2"><span className="text-xs font-bold text-cyan-700">Prontidão</span><span className="ml-2 text-xl font-black text-cyan-950">{score}%</span></div></section><div className="grid gap-6 lg:grid-cols-[1.1fr_.9fr]"><section className="space-y-4"><div className="rounded-3xl bg-white p-5 shadow-sm"><p className="text-xs font-black uppercase tracking-wider text-slate-400">Política institucional</p><input value={String(policy.title||'Política de IA da instituição')} onChange={e=>setPolicy(p=>({...p,title:e.target.value}))} className="mt-3 w-full rounded-2xl bg-slate-50 p-3 text-sm font-bold outline-none"/><div className="mt-4 grid gap-4 md:grid-cols-2"><TextList title="Sistemas autorizados" values={allowed} onChange={values=>setPolicy(p=>({...p,allowed_systems:values}))} placeholder="socratic_tutor, writing_coach"/><TextList title="Usos proibidos" values={prohibited} onChange={values=>setPolicy(p=>({...p,prohibited_uses:values}))} placeholder="decisão automática de alto impacto"/></div><label className="mt-4 block text-xs font-black uppercase tracking-wider text-slate-400">Contato para incidentes</label><input value={String(policy.incident_contact||'')} onChange={e=>setPolicy(p=>({...p,incident_contact:e.target.value}))} placeholder="responsavel@escola.br" className="mt-2 w-full rounded-2xl bg-slate-50 p-3 text-sm outline-none"/><button onClick={save} disabled={saving||!selected} className="mt-4 flex items-center gap-2 rounded-2xl bg-slate-950 px-5 py-3 text-sm font-black text-white disabled:opacity-50"><Save className="h-4 w-4"/>{saving?'Salvando...':'Salvar política'}</button></div><div className="rounded-3xl bg-white p-5 shadow-sm"><p className="text-xs font-black uppercase tracking-wider text-slate-400">Checklist de prontidão</p><div className="mt-4 grid gap-2 md:grid-cols-2">{Object.entries((readiness.checks||{}) as Record<string,boolean>).map(([k,v])=><div key={k} className="flex items-center gap-2 rounded-2xl bg-slate-50 p-3 text-sm font-bold text-slate-700">{v?<CheckCircle2 className="h-4 w-4 text-emerald-500"/>:<AlertTriangle className="h-4 w-4 text-amber-500"/>}{k}</div>)}</div></div></section><aside className="rounded-3xl bg-white p-5 shadow-sm"><p className="text-xs font-black uppercase tracking-wider text-rose-600">Incidentes e revisão</p><h2 className="mt-1 text-xl font-black">Acompanhamento operacional</h2><p className="mt-2 text-sm text-slate-500">Incidentes abertos: <b>{String(readiness.openIncidents||0)}</b></p><div className="mt-4 space-y-3">{incidents.length===0?<div className="rounded-2xl bg-emerald-50 p-4 text-sm font-bold text-emerald-800">Nenhum incidente registrado.</div>:incidents.slice(0,10).map((i,index)=><article key={String(i.id||index)} className="rounded-2xl bg-rose-50 p-4"><p className="text-[10px] font-black uppercase tracking-wider text-rose-600">{String(i.severity||'low')} · {String(i.category||'incidente')}</p><p className="mt-2 text-sm font-bold leading-5 text-rose-950">{String(i.description||'')}</p></article>)}</div></aside></div></main>}
function TextList({title,values,onChange,placeholder}:{title:string;values:string[];onChange:(v:string[])=>void;placeholder:string}){return <div><label className="text-xs font-black uppercase tracking-wider text-slate-400">{title}</label><textarea value={values.join('\n')} onChange={e=>onChange(e.target.value.split('\n').map(v=>v.trim()).filter(Boolean))} rows={5} placeholder={placeholder} className="mt-2 w-full rounded-2xl bg-slate-50 p-3 text-sm outline-none"/></div>}
